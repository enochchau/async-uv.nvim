local a = require("async")
return {check_start = a.wrap(vim.loop.check_start), close = a.wrap(vim.loop.close), fs_access = a.wrap(vim.loop.fs_access), fs_chmod = a.wrap(vim.loop.fs_chmod), fs_chown = a.wrap(vim.loop.fs_chown), fs_close = a.wrap(vim.loop.fs_close), fs_closedir = a.wrap(vim.loop.fs_closedir), fs_copyfile = a.wrap(vim.loop.fs_copyfile), fs_event_start = a.wrap(vim.loop.fs_event_start), fs_fchmod = a.wrap(vim.loop.fs_fchmod), fs_fchown = a.wrap(vim.loop.fs_fchown), fs_fdatasync = a.wrap(vim.loop.fs_fdatasync), fs_fstat = a.wrap(vim.loop.fs_fstat), fs_fsync = a.wrap(vim.loop.fs_fsync), fs_ftruncate = a.wrap(vim.loop.fs_ftruncate), fs_futime = a.wrap(vim.loop.fs_futime), fs_lchown = a.wrap(vim.loop.fs_lchown), fs_link = a.wrap(vim.loop.fs_link), fs_lstat = a.wrap(vim.loop.fs_lstat), fs_lutime = a.wrap(vim.loop.fs_lutime), fs_mkdir = a.wrap(vim.loop.fs_mkdir), fs_mkdtemp = a.wrap(vim.loop.fs_mkdtemp), fs_mkstemp = a.wrap(vim.loop.fs_mkstemp), fs_open = a.wrap(vim.loop.fs_open), fs_opendir = a.wrap(vim.loop.fs_opendir), fs_poll_start = a.wrap(vim.loop.fs_poll_start), fs_read = a.wrap(vim.loop.fs_read), fs_readdir = a.wrap(vim.loop.fs_readdir), fs_readlink = a.wrap(vim.loop.fs_readlink), fs_realpath = a.wrap(vim.loop.fs_realpath), fs_rename = a.wrap(vim.loop.fs_rename), fs_rmdir = a.wrap(vim.loop.fs_rmdir), fs_scandir = a.wrap(vim.loop.fs_scandir), fs_sendfile = a.wrap(vim.loop.fs_sendfile), fs_stat = a.wrap(vim.loop.fs_stat), fs_statfs = a.wrap(vim.loop.fs_statfs), fs_symlink = a.wrap(vim.loop.fs_symlink), fs_unlink = a.wrap(vim.loop.fs_unlink), fs_utime = a.wrap(vim.loop.fs_utime), fs_write = a.wrap(vim.loop.fs_write), getaddrinfo = a.wrap(vim.loop.getaddrinfo), getnameinfo = a.wrap(vim.loop.getnameinfo), idle_start = a.wrap(vim.loop.idle_start), listen = a.wrap(vim.loop.listen), new_async = a.wrap(vim.loop.new_async), new_work = a.wrap(vim.loop.new_work), pipe_connect = a.wrap(vim.loop.pipe_connect), poll_start = a.wrap(vim.loop.poll_start), prepare_start = a.wrap(vim.loop.prepare_start), random = a.wrap(vim.loop.random), read_start = a.wrap(vim.loop.read_start), shutdown = a.wrap(vim.loop.shutdown), signal_start = a.wrap(vim.loop.signal_start), signal_start_oneshot = a.wrap(vim.loop.signal_start_oneshot), tcp_close_reset = a.wrap(vim.loop.tcp_close_reset), tcp_connect = a.wrap(vim.loop.tcp_connect), timer_start = a.wrap(vim.loop.timer_start), udp_recv_start = a.wrap(vim.loop.udp_recv_start), udp_send = a.wrap(vim.loop.udp_send), walk = a.wrap(vim.loop.walk), write = a.wrap(vim.loop.write), write2 = a.wrap(vim.loop.write2)}